# v1.3.0
## refactor:
### 创建BasePage组件:
   - 封装 Scaffold + GestureDetector + 背景渐变 + SafeArea
   - 可选 AppHeader（支持 title 和 titleWidget）
   - 点击空白区域失焦
   - 响应式内容区域（maxWidth 限制）
   - 支持 child 或 builder 两种内容构建方式

## fix:
    - 修复更新应用数据丢失问题
    - 集中修复了analyze发现的info级问题